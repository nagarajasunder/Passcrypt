<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Passcrypt" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_200</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/purple_500</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="MytextColor" parent="@android:style/TextAppearance">
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="credInputStyle" parent="Theme.AppCompat">
        <item name="backgroundColor">@color/purple_500</item>
        <item name="background">@color/purple_500</item>
    </style>

    <style name="cardItem" parent="Widget.MaterialComponents.CardView">
        <item name="cardBackgroundColor">@color/dark_mode</item>
        <item name="cardCornerRadius">8dp</item>
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
        <item name="cardElevation">8dp</item>
    </style>

    <style name="viewCredential" parent="@android:style/Widget">
        <item name="android:background">@color/dark_mode</item>
    </style>
</resources>